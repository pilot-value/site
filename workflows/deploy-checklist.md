# デプロイ前チェック — 何を守っているか

**この文書は [CLAUDE.md](../CLAUDE.md) から切り出したもの。** 走らせ方（`node check.mjs`）は
CLAUDE.md 側にあり、ここにあるのは **各検査が何を約束しているか**。

⚠️ **検査が落ちたら、直す前にここを読む。** どれも過去に実際に壊れた形を1つずつ書き留めたもので、
**検査のほうが正しいことがほとんど**。その画面を触るときも先に読む。

⚠️ 番号は歴史的なもの（手で1本ずつ流していた頃の順）。今は `node check.mjs` が速い順に並べ替えて回す。

---

## デプロイ前チェック
1. `node serve.mjs` を起動する（下の assert 系は localhost 必須）
2. `node check-salary.mjs` — 全ページ × SSOT の整合。2パス構成:
   - パス1 = 110社の航空会社ページに自社の機長／副操縦士平均が載っているか
   - パス2 = **サイト全 HTML**（トップ・記事・比較ページ含む）から「社名＋役職＋金額」を拾って SSOT と突合。
     `❌` はレンジ外＝誤り。`⚠️` はレンジ内だが節目でない＝要目視。
     複数社の平均をまたぐ言い方など、正しいと確認済みのものは `check-salary.mjs` の `ALLOW` に理由つきで置く
   - `❌` が1件でもあれば `exitCode 1` になる（`salary-data.json` の不一致も同じ）
3. `node check-sources.mjs` — 出所台帳 [salary-sources.mjs](salary-sources.mjs) の整合（語彙・孤児 slug・`in_use` の必須項目・`conf:'high'` なのに出所ゼロの社）。
   `❌` だけが `exitCode 1`。`⚠️` は「まだ出所が埋まっていない」ことを示すだけなので落とさない。
   出所を足した回は `node check-sources.mjs --online` でURLの生存も確認する
4. `node assert-jobs.mjs` — 載せている求人がまだ生きているか。
   締切が過ぎたのに「募集中」のまま／一覧の更新日が90日以上前／JSON-LD の `validThrough` が過去、を静的に見る。
   `--online` は掲載元を実際に叩き、消えた求人（`/jobs/warning` へ飛ぶ）を落とし、
   生きている求人は**掲載元の見出しを並べて出す**。
   ⚠️ 掲載元は URL の番号を**別の求人に付け替える**。番号が生きていても中身が入れ替わっているので、
   見出しはこちらのカードと目で見比べる（2026-08-15、9本中3本が別の求人を指していた）
5. `node assert-jp.mjs` — 日本語ページの通貨表示・JPY⇄USD 往復
6. `node assert-currency.mjs` — 英語ページの通貨バグ検出
7. `node assert-langtoggle.mjs` — 言語切替の allowlist と往復
8. `node assert-review-quality.mjs` — 口コミの品質判定。**本物の投稿が長さで弾かれないこと**と、
   同じ判定を持つ**5か所（`submit-review.html` / `en/submit-review.html` / `mail-bot/admin-notify.mjs` /
   `mail-bot/delete-review.mjs` / `supabase/functions/notify-admin/index.ts`）が同じ答えを返すこと**を確かめる。
   コピーを持たず各ファイルから実体を切り出して動かすので、1か所だけ直すと落ちる
9. `node assert-translate-review.mjs` — 口コミ自動翻訳（`translate-review`）の形の検証。
   モデルが指示と違う形（配列・コードフェンス・前後に説明文）で返しても壊れないこと、
   原文の言語判定が比率で効くこと（英語の本文に地名が1語混じっても `en` のままであること）、
   訳文から数字が落ちたのを拾えること。**ネットワークも API キーも使わない**。
   [supabase/functions/translate-review/index.ts](supabase/functions/translate-review/index.ts) を
   Node 24 の `.ts` 直読みでそのまま import するので、本体を直すとここも一緒に動く。
   **鍵が死んだときに管理者へ知らせる判定（`isFatalKeyError`）も見る。**
   これは `translate-review` と `parse-payslip` の2つが同じものを持つので、
   両方から実体を import して同じ答えを返すか突き合わせる（片方だけ直すと落ちる）
10. `node db/test-announce.mjs` — 会員へ出すお知らせメール（[mail-bot/announce-mail.mjs](mail-bot/announce-mail.mjs)）の検査。
   **金額・会社名・明細の項目名が1つも入らないこと**と、
   本文の主張がサイトの実装とずれていないこと（解放「90日」＝`db/pay-reports.sql`、
   項目の見出し＝`pay-report.html` の `renderResult`）を突き合わせる。
   pay-report の文言を変えるとここが落ちる。ネットも鍵も使わない
11. `node assert-conditions.mjs` — 給与レポート直後の待遇モーダルが約束どおり閉じられるか。
   通常ログインでは出ない／× ・背景・ESC のどれでも閉じる／閉じても下のページが残る／
   1問ごとに保存され、1問目で閉じても答えた分は残る／× のときは**見せていた1問だけ**
   スキップとして残す（見せていない残りは記録しない）／初回3問・2回目1問でその次は出ない。
   Supabase を丸ごと差し替えて本物の [pv-conditions.js](pv-conditions.js) を描くので、
   **localhost が要る。本番の DB には触らない**（見た目は `node shot-conditions.mjs` が撮る）
12. `node assert-referral.mjs` — 招待（データ密度ループ）の約束。
   **同じ区分に2人以下しか居ないとき、画面に数字が1文字も出ないこと**、3人・4人のときだけ
   「あと2人／あと1人」が出ること、5人そろったら招待の導線が消えること、
   `?ref=` が Google ログイン・6桁コード・言語切替をまたいで残ること、
   待遇モーダル（`[data-pvc]`）と重ならないこと（こちらは本物のモーダルを作らない）。
   招待された人の着地は**画面の中央に出る1枚**だが、`position:fixed` でも `role="dialog"` でもなく
   `absolute` の 100vh。**スクロールを止めない・nav を覆わない・閉じ方が3つある**
   （× ／ カードの外 ／ ESC）ので閉じ込めが起きない。ここを本物のモーダルにすると落ちる。
   **送る文面に VISION（PILOT VALUE を名乗る／職業の価値／完全匿名／タグライン）が入っていること**も見る。
   ここは招待された人がサービスを見る唯一の入口で、「便利なサイトがある」だけの文面に戻すと落ちる。
   文面に勤務先名・金額・自分のレポートURLは入れない（変種を足すと勤務先名が入る道が生える）。
   **マイページ（`profile.html`）の常設入口が条件に関係なく出ること**も見る。
   給与を1件も出していない人にも・5人そろっている人にも出て、
   人数（招待した数・成立数・残り何人）は1文字も出さない。
   ここが消えると「招待したい」と思った人の行き先がサイトから無くなる（2026-08-19 に実際そうなっていた）。
   ⚠️ 偽物 Supabase の `rpc` は本物と同じ「then だけを持つ箱」。async に戻さない。
   **localhost が要る。本番の DB には触らない**（見た目は `node shot-referral.mjs` が撮る）
13. `npm run test:sql` — Supabase 側を触った場合。
   [db/test-referrals.mjs](db/test-referrals.mjs) もここで走る
   （紹介者は一生1人・自己招待が通らない・2人以下の区分では数字を返さない）。
   [db/test-pay-rows.mjs](db/test-pay-rows.mjs) もここ
   （1行＝1人の読み出し口。k≧5・準識別子ゼロ・p10-p90 クリップ・有効数字2桁・30日遅延の5つ）
14. `node assert-no-pii.mjs` — オーナーの身元が漏れていないか（下記「公開リポジトリであること」を参照）。
   6つの検査がある: A) 追跡テキスト（`.svg` 含む）の中身 B) 画像・PDF のメタデータ（GPS と作者欄）
   C) 本番で配信される拡張子のホワイトリスト D) git の作者（メール **と表示名**・設定と履歴の両方）
   E) 本番プローブ（`--live` のみ） F) **git 履歴の中身**（`--history` のみ）。
   push 後にもう一度 `node assert-no-pii.mjs --live` を走らせる。
   **`.githooks/pre-commit` から毎回自動で走るので、通常は意識しなくてよい**（下記の初回セットアップが要る）。
   ⚠️ **A) が通ることは「履歴が綺麗」を意味しない。** A) は今のファイルしか見ない。
   公開リポジトリでは過去のコミットも `raw.githubusercontent.com/<org>/<repo>/<古いSHA>/<ファイル>`
   で誰でも取れる。2026-08-09 に絶対パスを全部消して A) は 0 件になったのに、
   **履歴には実名が残ったままで実際に外から取れていた**（2026-08-20 に F) を足して発見）。
   履歴を触ったとき・公開する前・Mac を替えたときは `--history` を手で走らせる
   （毎回の commit では重いので走らせない）。
   見つかっても**その行を直して commit しても消えない**。直し方はリポジトリの作り直しだけ
15. `node assert-header.mjs` — ヘッダーが幅ごとに正しく畳まれるか（**日英14ページ × 10幅**）。
   ヘッダーは Tailwind の `md:` で出し分けるのをやめ、**[search.js](search.js) が実測して自動で畳む**。
   `.pv-nav-links` / `.pv-nav-right` / `.pv-nav-2nd` / `.pv-nav-cta` は**JS が付ける目印**なので、
   HTML 側に `md:` を書き足しても効かない。
   ★**ヘッダーは2種類ある** ── `<nav id="main-nav">`（サイトの56枚）と
   `<header class="mr-top">`（マイページ系8枚＋認証4枚＝**12枚**）。**`search.js` が両方に ≡ と引き出しを付ける**
   （`inject()` が `#main-nav || header.mr-top`）。文言を2か所に持たないため新しい JS を作らない。
   畳む段のセレクタは `#main-nav` 限定なので `.mr-top` には当たらず、≡ と引き出しだけが付く。
   検索窓も生えない（`buildUI()` が `.flex.items-center.gap-3` を要る）。
   ★**認証4枚（`login` / `signup` の日英）も 2026-08-27 に `.mr-top` へ揃えた。**
   それまでは `body` 直下に `position:fixed` の `<div>` を2つ置くだけの**第3のヘッダー**で、
   `<header>` も `<nav>` も無い＝`search.js` の `inject()` が即 return し、**≡ も引き出しも
   原理的に出なかった**（🇯🇵 JP と ¥ JPY だけ並んで見えたのは、`lang-toggle.js` /
   `currency.js` が `#theme-toggle` の隣に自分で差し込むから。ヘッダーがあったからではない）。
   `assert-header.mjs` にも `shot-header.mjs` にも入っておらず、**一度も見られていなかった**。
   骨格は HTML に書き写さず `pv-tokens.css` → `my-value.css` の2本を読む（[my-value.css:12](my-value.css#L12) の禁止事項）。
   カードを `<main class="auth-main">` で包み、中央寄せを `body` からそちらへ移した
   ── `body` に `padding` を残すと `.mr-top` の背景が左右の端に届かない。
   ⚠️ この4枚を `assert-header.mjs` に足すとき **`login:true` を付けない**。
   セッションがあると `login.html` がマイページへ飛ばしてヘッダーごと消え、必ず落ちる。
   ★**畳む順は4段** ── ① 真ん中のリンク群 → ② ログイン・← 戻る → ③ CTA →
   ★④ 検索窓（`pv-nav-micro`。320px 用）。**言語と通貨は最後まで残す**
   （外貨で見ている人の主機能。ここを畳むと海外の人が円のまま取り残される）。
   ⚠️ 段④だけは `BREATH`（20px のすき間）を見ない。320px でそれは原理的に取れないし、
   見ると 375px でも検索窓が消える。**本当に入らないときだけ**消す。
   ★**`id="main-nav"` を持つページには必ず `search.js` を読ませる。**
   読ませないと `white-space:nowrap` も畳む仕組みも ≡ も**丸ごと効かない**。
   2026-08-27、`pay-report` / `submit-review` の日英4枚が実際にそうなっていて、
   iPhone で「← 世界の航空会社」が4行に折れて本文へ 109px 垂れていた。
   ★**`viewport-fit=cover` を足さない。** `env(safe-area-inset-*)` が 0 なのは正しい
   （cover を付けていない iOS は最初からホームバーの内側にページを置く）。
   ★**「画面の下が切れる」の正体は2つある。どちらも「箱が窓より広くなる」形。**
   ① ページが横に溢れると iOS はレイアウト幅を広げ、`position:fixed;left:0;right:0` は
   その**広げられた幅**に貼りつく＝見えている窓から左右にはみ出す。
   ★② **入力欄の文字が 16px 未満だと、iOS はそこに触れた瞬間ページごと拡大する**（**戻らない**。
   こちらから呼ぶ `focus()` でも起きる ── 「匿名で提出」は足りない欄へ飛んで focus する）。
   拡大しているあいだ見えている窓はレイアウト幅より狭いので、同じことが起きる。
   ⚠️ **①が全部 ✓ でも②で切れる。**2026-08-27、手元では 390px で `scrollWidth === clientWidth`、
   バーもぴったり `[0,393]` だったのに、オーナーの iPhone 16 では左右とも切れていた。
   写真から拡大率を実測（45.6px の入力欄が 116px。拡大なしなら 107px）＝**1.09 倍**。
   手当ては各ページの CSS に `@media (pointer:coarse),(max-width:820px){…{font-size:16px}}`。
   **22ファイル**（`.form-input` / `.auth-input` / `.search-input` / `.ap-sel` / `.ap-q` /
   `.pvcf-*` / `#pv-search-input` / `.lvl-drop-input`）に入れてある。**16px を下げない。**
   見るのは、はみ出さない／2行に折れない／隣どうしが 20px 未満に近づかない／
   ≡ は畳んだときだけ出る（`.mr-top` の4枚は**常に**出る）／CTA が黙って消えない／
   引き出しに**そのページのリンクが入り、国別年収は入らず、口コミへ行ける**こと／
   ★**ページが横に溢れていない**（`scrollWidth === clientWidth`）。
   ★**触れる入力欄が 16px 未満でない**（入力欄のある22枚を 390px で1周。上の②）。
   ⚠️ 畳めているかは `scrollWidth` では測れない（文字が折れて逃げるあいだ増えないので
   入っていないことが分からない）。だから**箱の座標**と `scrollWidth` の両方を見ている。
   ⚠️ 幅の一覧に **320px（iPhone SE 1 / 5s の実幅）**が入っている。
   すき間（`MIN_GAP`）は **390px 未満では見ない** ── そこで要るのは
   はみ出さないことと2行に折れないことだけ。
   ⚠️ 横の溢れは**カードの中身が原因でも起きる**。`.pv-grid-4` の `1fr` は最小が min-content なので、
   カードの min-content が1列ぶんを超えるとグリッドごと右へ膨らむ
   （2026-08-27、`/en/` の口コミカードが 1000〜1024px で 28px 溢れていた。
   [lp.css](lp.css) の `.pv-voice-head` の `flex-wrap:wrap` がその手当て）。
   **localhost が要る**（見た目は `node shot-header.mjs` が撮る）
16. `node assert-admin.mjs` — 管理者ページが「ログインした管理者にしか見えない」こと。
   2026-08-20 まで `admin.html` は合言葉をページ本文に持っていて、しかも画面から `profiles` を
   直接読んでいた。**ページは誰でも読めるので合言葉は最初から公開されていたのと同じ**で、
   開発者ツールから会員の氏名・メール・生年月日・在籍企業が全部取れた。
   直した形は「ページは合言葉を持たない／管理者かはサーバーが答える（`pv_is_admin`）／
   一覧は `admin_list_profiles` `admin_list_reviews` からしか来ない」。
   見るのは、日英2ページに合言葉・パスワード欄・`from('profiles')` が残っていないこと、
   [db/admin.sql](db/admin.sql) が名簿・入口・`profiles` の締めを全部持っていること、
   実際に開いて ①ログアウト ②管理者でない ③管理者 ④SQL 未適用 の4通りが正しく分かれること。
   ⚠️ **DB 側が本体。** `db/admin.sql` を Supabase で流すまでページだけ直っても意味が無い
   （anon キーは全ページに埋まっていて回せないので、RLS で締める）。
   **localhost が要る。本番の DB には触らない**（Supabase ごと差し替える）

17. `node assert-unlock.mjs` — **2つの鍵が混ざらないこと**。
   口コミを1件出すと「全社の口コミ」が読める（2026-08-22 に**期限を外した**＝ずっと読める）。
   給与明細を1枚出すと「年収データ」が90日読める。**この2つは別々の鍵で、口コミの鍵で年収枠は開かない。**
   昔あった引き継ぎ（`grandfatherSalaryUnlock` — 口コミの鍵の期限を年収の鍵に書き写す）は削除済み。
   これが復活すると**口コミ1件で年収データがずっと開く**ので、①がそれを見張る。
   見るのは、口コミの鍵だけ持つ人・サーバー側に口コミがある人・明細だけ出した人の3通りで
   年収枠と口コミ枠が正しく分かれること、口コミの鍵を書く**9か所すべて**が
   `pv-session.js` の `PVUnlock.reviewUntil()`（＝遠い未来）を使っていること
   （増やしたら未知の書き手として落ちる）、年収側は今も90日で `access_until` 由来であること、
   そして**画面・メール・SEO の元表に「30日／1ヶ月」の約束が1つも残っていないこと**（日英）。
   ⚠️ 文言を消す一括スクリプトは [patch-unlock-wording.mjs](patch-unlock-wording.mjs)（42か所・冪等・`--check` あり）。
   本物の30日／1ヶ月（台湾のホテル・IPハッシュの保持・預かり証・ログイン最長・退会後の削除・
   有給15〜30日・口コミ本文の引用）は消さない。除外は同スクリプトと `assert-unlock.mjs` の `ALLOW` に理由つきで置く。
   **localhost が要る。本番の DB には触らない**（Supabase ごと差し替える。見た目は `node shot-unlock.mjs` が撮る）

18. `node assert-admin-notify.mjs` — 管理者への通知メール（[notify-admin](supabase/functions/notify-admin/index.ts)）の検査。
   1本の Edge Function が**6つの表**を捌く（口コミ／新規会員／問い合わせ／給与レポート／
   登録前の預かり／待遇アンケート）。見るのは6つ:
   ① **給与まわりのメールに金額・明細の項目名が1文字も出ないこと。**
     給与レポートは `user_id` を持たない設計で「誰がいくら」を運営側に残さない（[db/pay-reports.sql](db/pay-reports.sql)）。
     メールに載せると受信箱と Resend の送信ログに残る＝設計で守ったものを送信で外に出す。
     待遇の金額回答と自由記述（スキーマが「非公開」と書いている列）も同じ扱い
   ② **`builders` の顔ぶれと [db/notify-admin-webhooks.sql](db/notify-admin-webhooks.sql) の表の配列が一致すること。**
     いちばん静かな壊れ方はこれ。片方だけ足すと実装は正しく見えるのに一通も届かない
   ③ 待遇の**スキップの行では送らない**こと（スキップも本物の行として保存されるので、
     飛ばすたびに中身の無いメールが飛ぶ）
   ④ 「明細から／手入力」の言い方が [db/usage.mjs](db/usage.mjs) と揃っていること
   ⑤ 待遇の**累計（n / 32問）を数えられなくても通知そのものは落ちない**こと。
     累計はおまけで、値打ちは「どの質問にどう答えたか」の側にある。
     数えられないときは行ごと省く（「0 / 0問」のような嘘の数字を出さない）
   ⑥ **`db/notify-admin-webhooks.sql` が壊れていないこと**（`$$` が対・確認クエリが1つだけ）。
     オーナーが Supabase に貼るまで構文エラーに気づけないので、ここで見る
     （上の「新しいスクリプトを書く前に」の `String.replace` の罠を参照）
   本体（`.ts`）を Node 24 の直読みでそのまま import し、`fetch` だけ差し替える。
   **ネットも鍵も localhost も使わない**

19. `node assert-pay-rows.mjs` — **REAL PAY**（[actual-pay.html](actual-pay.html)）の検査。
   この画面は**1行＝1人**の匿名レポートを出す。**給与を出した人は全員1行ずつ出る。**
   ★2026-08-24、マイページを3枚に分けた（左メニューの並びもこの順）:
   **REAL PAY**（他人の実給与を1行ずつ）→ **DEEP PAY**（何で構成されているかを複数投稿から集計）→
   **VERIFIED PAY**（明細に裏付けのあるものだけを集計）。
   **後ろ2つはまだページが無い。`<span aria-disabled="true">` で置き、`href` を書かない**
   （`href` の無い `<a>` はキーボードから触れないのに押せそうに見える＝いちばん悪い形。
   リンクにすると `assert-links.mjs` が 404 で落ちる）。
   ★2026-08-23 オーナー判断で **k≧5 の門・30日の遅延・p10-p90 のクリップ・公開情報の推定レンジ（青）** を
   外した。だから守りは残る7つに全部かかっている（理由は [db/pay-rows.sql](db/pay-rows.sql) の契約ヘッダ）:
   ①鍵（明細を1枚出した人だけ・90日・サーバー側）②準識別子ゼロ ③有効数字2桁
   ④1行＝1人（同じ人の複数月は年換算の中央値で1行）⑤引数ゼロ
   ⑥並びはサーバーが決める（**新しい順・新しいほうが上**）⑦常識の幅（年 $10,000〜$700,000 の外は
   打ち間違いとして出さない）。
   ⚠️ **2026-08-24、②を1段ゆるめた。**オーナー指示で「投稿の時期」を出すことにしたので、
   ②は「投稿の日付を返さない」から**「投稿の日付そのものは返さない（粗い5段だけ返す）」**になった。
   返るのは 0〜4 の番号だけで、日付も年月も返らない（理由は [db/pay-rows.sql](db/pay-rows.sql) の契約ヘッダ）。
   ⚠️ **2026-08-25、⑥も1段ゆるめた。**オーナー指示「あと一旦出した順にしてもらっていいよ」→
   同日「これは新しい順にしよう」。前は `md5(人のキー)` 順＝並びに時間がまったく無い形だった。
   今は**新しい順**（`order by p.last_at desc, md5(p.pkey)`）。
   段（5段）を出した時点で「だいたいいつ」は既に読めるが、**同じ段の中でもどちらが先かが読める**ようになった。
   さらに新しい順は**いちばん最近出した人が必ず先頭に来る**ので、古い順よりもう一段だけ弱い
   （出した直後に見た人には、1行目が自分の直前の1人だと分かる）。
   それでも並べるのに使う時刻は行に入れていないし、画面から並べ替える口も無い。
   戻すのは `db/pay-rows.sql` の `order by` を `order by md5(p.pkey)` に書き戻すだけ
   （古い順に戻すなら `desc` を外す）。
   **同じ会社・同じ職位の同僚には当てられうる**水準であることは承知のうえで選んだ形。見るのは:
   - **鍵の無い人に金額が1文字も出ない**（サーバー側で止める。画面のモザイクではない）
   - **表は1枚だけ。**粒度を2つに分けた形に戻さない（同じ人が両方に出て、二重に数えたように見える）
   - 表示中の**すべての金額が表示通貨で有効数字2桁**。k≧5 もクリップも無いので、**この丸めがいちばん外側の守り**
   - **機材（`fleet`）は読んでも返してもいない**（2026-08-24。個人特定を避けるオーナー判断。
     機材で絞る口も無い。列に出していないものを絞れると逆算できる）
   - 基地コード・在籍年数・年代・**投稿の日付**・原本通貨・契約形態・国籍・`proof_hash` が1つも出ない。
     ★時期は**5段の番号だけ**が来る（下の「投稿時期」）。生の日付は今も毒で、
     検査は `2026-08-05` のような文字列が画面に出ないことを見張っている
   - ★**自由入力で打ち込まれた社名は、そのままでは1文字も出さない**（2026-08-25）。
     オーナー指摘「REAL PAY の『その他の航空会社』ってなに？失礼じゃない？ちゃんと航空会社名書いて」。
     `pv_airline_resolve()` が打ち込まれた文字列を**112社の語彙に当てて**コードに直す。
     当たれば普通に社名が出る。当たらなければ `'other'`。
     **返るのは必ず `pv_airlines.code` か `'other'` のどちらか**なので、打ち込まれた生の文字列は外に出ない。
     照合は記号・空白・大小・括弧書きを落としたうえでの**完全一致だけ**（部分一致にしない。
     人の行に違う会社を貼るほうが、社名が出ないより悪い）。
     ⚠️ ここは口コミ側にも効く。`reviews_v2.airline` には**打ち込まれた文字列がそのまま入りうる**
     （[submit-review.html](submit-review.html) の `effectiveAirline`）ので、口コミ由来の行も必ず通す。
     当たらなかった行の札は**「一覧にない航空会社」/ "Airline not listed"**。
     ⚠️ 本番に1件、打ち込んだ社名を保存していない古い行があり、これは何をしても札のままになる
   - **並び替えの口を作らない**（`sort` / `新しい順` / `order` の select が日英の HTML と JS に無い）。
     並びを決めるのはサーバーだけ（新しい順）。画面に口を作ると、金額順のランキングにされる
   - **金額での並べ替え・「✓ Verified だけ」の絞り込み・賞与の列・「賞与ありのみ」が無い**
     （並べ替えはこの画面をランキングにする。あとの3つは絞った行数がそのまま生の人数になる）
   - **原本通貨（¥ € AED SGD …）を混ぜない。表示通貨に揃える**（原本通貨は②の返さないもの）
   - **絞り込みが行き止まりにならない**（選択肢は実際に行がある区分からしか作らない・総当たりで確認）。
     **会社を打ち込む窓（`ap-q`）もここに含める**。打ち込みで消えた会社の選択は自動で外す
     （残すと「選んだのに0件」になる）
   - 通貨を切り替えても **RPC を引き直さない**（データは手元に持って描き直すだけ）
   - **外した約束が文言に戻っていない**（日英とも本文に「5人」「30日」「推定」が1つも無い）。
     ⚠️ これは静かに戻る。「5人そろうと出ます」は今は嘘
   - **青（推定）の見た目がこの画面に1つも無い**（`.ap-badge--pub` / `--pv-blue-*` / `.ap-range`）
   - **サイドバーが8枚（4画面 × 日英）で1バイトも食い違わない**
   - **登録前の預かり（`pay_reports_pending`）も同じ一覧に混ざる**（2026-08-23）。
     まだ会員になっていない人のぶんが1行も出ないと「出したのに載っていない」に見える。
     ★`claimed_at is null` のものだけ読む（本棚へ移った人は本棚側に居るので、外すと二重に出る）。
     年換算は `pv_pending_usd()` が出すが、**式は書き写さず `pv_annual_total()` を呼ぶ**
     （書き写すと同じ明細から本棚と預かりで違う年収が出る。`db/test-pay-rows.mjs` の
     ▼12 が同じ payload を両方に通して突き合わせている）。この関数は**誰にも grant しない**
   - ★**口コミに書かれていた給与も同じ一覧に混ざる**（2026-08-24 オーナー指示
     「過去に口コミに投稿してくれたパイロットたちも入れられるんじゃない？」）。
     本番の口コミ14件のうち**8件が給与を持っていて、うち1人は明細も出していた**ので**+7行**。
     **口コミフォームはもう金額を集めていない**（[submit-review.html](submit-review.html) が
     「金額はここでは集めない」）ので、**この7行が打ち止め。将来増えない**。
     その金額は**すでに口コミカードに出ている**ので、混ぜても新しく外へ出る情報はゼロ。見るのは:
     - **人の突き合わせは対応表 `pv_review_person` が1つで持つ**。口コミ側の塩（`::pv_anon::`）と
       給与側の塩（`::pv_pay::`）は違うので、名簿からハッシュを作り直して一度だけ結ぶ。
       ⚠️ この表は **RLS を有効にしてポリシーを1つも置かない**＋`revoke all`。
       security definer の関数からしか読めない
     - **明細を優先する。**同じ人が明細も出していたら口コミ側は落ちる（契約④が破れる）
     - 金額の式は**口コミカードとまったく同じ順**（年収 ＞ 基本＋乗務手当＋賞与 ＞ 月給×12＋賞与）。
       違う式にすると**同じ人の金額が画面ごとに変わる**
     - 出典は明細と同じ **「本人記録」**（オーナー決定。札を3種類に増やさない）
     - ⚠️ **`reviews_v2` に列を足さない。**[airlines/airline-reviews-ui.js](airlines/airline-reviews-ui.js) が
       `select('*')` で読んでいるので、足した列はそのまま公開される
     - ⚠️ 換算は**今のレート**（口コミは投稿時のレートを持っていない）。
       明細側の `annual_total_usd` は投稿した瞬間に確定するので、そこだけ性質が違う
   - **⑦常識の幅**（`usd between 10000 and 700000`）。k≧5 もクリップも無くなったので、
     打ち間違いを止めるのはここだけ。実際に本番にあったのは「年 $0.75（＝¥110）」と
     「月額の欄に年額 ¥1,200万 を入れた行（＝$918,486）」の2つ。
     ⚠️ **上限は判断であって保証ではない。**$70万超の本物が来たら、その1件を確かめてから動かす
   - **列は6つ**（ロゴ＋会社 / 職位 / 年収 / 月あたり / 出典 / 投稿時期）。見出しは `年収`。
     ★「（丸め）」と書かない（オーナー指定。丸めていることは表の下の1文が言う）。
     **「月あたり」は画面に出ている年収を12で割った数**であって、生の年収から割らない
     （生から割ると「$110K なのに月 $8.8K」のように、掛け算して合わない数字が出る）。
     ロゴは [airline-logos.js](airline-logos.js) を **`actual-pay.js` より前**に読む。
     忘れても落ちないが全社が頭2文字の札になる。`alt=""` にして社名は必ず文字でも出す
   - ★**数え上げを出す**（2026-08-24。ここは方針が変わったところ）。
     オーナー判断「本当の数字だけ出す」。**出した人に「どれだけ集まっているか」が見えないと
     Give & Get が成立しない**という理由で、前の「合計件数・カバー社数を出さない」を外した。
     ⚠️ **正直に書いておく。新しく外へ出るのは「今どれだけ集まっているか」と
     「直近1ヶ月でどれだけ増えたか」の2つ**（行数と会社数は前から数えれば分かった）。見るのは:
     - **上のカードは3枚**（実給与の投稿 / 航空会社 / 1ヶ月以内の新規投稿）。
       ★2026-08-25、3枚目を「今月の新規投稿」から改名した（オーナー指示）。
       **窓も暦の月から直近1ヶ月に変えてある。** 暦の月は毎月1日に 0 へ落ちるので、
       月初に来た人には誰も出していないように見えた。
       ⚠️ **カードの窓は、表のいちばん右「投稿時期」の段0（1ヶ月以内）と同じ境目**
       （どちらも `now() - interval '1 month'`）。**片方だけ直すと静かにずれる**
       ── 札は「1ヶ月以内 25件」なのに表で「1ヶ月以内」と出る行がそれより少ない、という形。
       `assert-pay-rows.mjs` が両方が同じ字であることを見ている
       ★2026-08-24 に4枚から減らした。オーナー指示「『13人 一覧のパイロット』いらない。
       件数だけでいいよ」。**「一覧のパイロット」を戻さない**（件数とほぼ同じ数を2度言うだけ）。
       3枚のうち1枚（航空会社）は `rows` を数えるだけ、残り2枚は `pv_pay_rows()` の `stats` から来る
     - **カードには丸い薄橙の下地＋線画の絵が1枚ずつ入る**（2026-08-24 オーナー指示）。
       インライン SVG・`currentColor`・ブランドのトークン色。外部ファイルもフォントアイコンも使わない。
       ⚠️ 絵と一緒に**「多くのパイロットが参加中！」のような一言を足さない**（数字を盛らない）
     - **数が読めないカードは、そのカードごと出さない**。`db/pay-rows.sql` を Supabase に
       貼るまで本番は `stats` を返さないので**1枚（航空会社）だけ**並ぶ。**埋めるための 0 を置かない**
     - **「実給与の投稿」は必ず表の行数以上**（サーバーと画面が別々に数えていない証拠）。
       `stats` は行と**同じ材料**（`sane`）から数える
     - **カードは絞り込みでは動かない**（ここは全体の話。絞った結果はページ送りが持つ）
     - ★**鍵が無い人・0件の人にも帯を出す**（2026-08-25 オーナー判断で反転）。
       前は「会員規模を先に見せない」で伏せていた。**まだ出していない人にこそ
       「どれだけ集まっているか」が見えないと Give & Get が始まらない**ので開けた。
       サーバーは `state:'locked'` でも `stats` と `give` を返す。
       **`rows` は空の配列のまま**（伏せた行ではない。ここが緩んだら
       `db/test-pay-rows.mjs` の 36・37 が落ちる）
     - **「直近30日で +X件」は出さない**（増え方の速さまでは出さない）。
       ★禁じているのは**増分の言い方**であって、数そのものではない。
       「1ヶ月以内の新規投稿 25件」は1ヶ月の窓で数えた**件数**なので、これに当たらない
     - **表の中には今までどおり「◯件」「◯人」を1つも置かない**
   - **ページ送りは10件ずつ。「全N件中 1〜10件を表示」＋数字のページ番号**（2026-08-24）。
     N は**絞り込んだ後の行数**で、絞ると N も一緒に減る（総数が残ると嘘になる）。
     行き止まりが無いこと・絞り込みを触ったら1ページ目に戻ることを総当たりで見る
   - ★**いちばん右に「投稿時期」の列**（2026-08-24 オーナー指示「投稿の日付を入れよう。
     最近、1ヶ月以内、1〜3ヶ月以内とか。半年以内、とか一覧の一番右に」）。
     区分は**5段**（1ヶ月以内 / 3ヶ月以内 / 6ヶ月以内 / 1年以内 / それより前）。見るのは:
     - **サーバーが返すのは 0〜4 の番号だけ。**画面はそれを言葉に置き換えるだけで、
       日付を組み立てない（`new Date(` / `toLocaleDateString` / `getMonth` が
       `actual-pay.js` に1つも無いことを見る）
     - **時期で並べ替える口も、時期で絞る口も作らない**
       （`id="ap-age"` / `data-ap-age` / `S.fAge` が日英の HTML と JS に無いことを見る）
     - 段は**その人の最新の提出**から出す（同じ人の複数月は1行なので、その行の新しさは最新）。
       ★**並びに使う時刻とまったく同じもの**から出す。別の時刻で並べると、上から下へ段が逆流して
       「並びと右端の列が食い違う」画面になる（検査が逆流しないことを見ている）
     - ★**段 3（1年以内）と 4（それより前）は、今の本番には出ない。**
       いちばん古い会員登録が 2026-05-04＝サイトはまだ約4ヶ月なので、届くのは 0〜2 だけ。
       来年になれば普通に届くので**サーバーの5段はそのまま**にしてある。
       ⚠️ 撮影用の行（[shot-actual-pay.mjs](shot-actual-pay.mjs)）に 3・4 を作らない。
       作ると本番に存在しない絵を見せることになる（2026-08-25 に実際そうなっていて、
       オーナーから「それより前とかあるはずがない」と指摘された）
   - ★**図は1つも無い**（2026-08-24 オーナー指示「右の分布グラフはやっぱりなし。一旦消そう」）。
     年収の分布の棒も「あなたの位置」の破線も**消した**。`.ap-viz` `.ap-vcard` `.ap-plot` `.ap-bw`
     `.ap-bar` `.ap-you` `.ap-ax` `.ap-vsub` と2段組（`.ap-cols` / `.ap-main` / `.ap-side`）は
     CSS ごと落としてあり、表が本文の幅いっぱいに広がる。
     破線にしか使っていなかった **`my_pay_reports()` の呼び出しも消した**（この画面は本人の明細を引かない）。
     ⚠️ **消した であって 差し替えた ではない。**別の図を置かない。分布は DEEP PAY で作り直す
     （`pay-viz.css` / `pay-viz.js` / `pv_pay_comp` / `pv_pct5` はそのために残してある）
   - **見出しの下に日本語の説明を置かない・右に説明カードを置かない**（オーナー指定）。
     表の下の1文（何が載っていないか）は**残す**。あれは説明ではなく約束で、
     消すと「機種はどこ？」に答えるものが画面から無くなる
   - **経験年数・提出日・レポートID・「Verified だけ」の絞り込みは戻さない**
     （`ap-exp` / `ap-date` / `ap-id` / `verified-only` が日英の HTML と JS に無いことを見る）。
     ★「提出日」は**生の日付**のこと。上の「投稿時期」（5段）とは別物で、こちらは今も出さない
   ⚠️ 割合を作る3つ（`pv_pay_comp` / `pv_pct5` / `pv_pending_comp`）は**定義だけ残してある**（DEEP PAY 用）。
   `pv_pay_rows()` からは呼ばない。**誰にも grant しない**。
   ⚠️ 集計側（`pay_benchmarks`）の k≧5 は**今も生きている**。一緒に外さない（ここが見張っている）。
   ⚠️ サイドバーを配るのは [patch-side-nav.mjs](patch-side-nav.mjs)。**HTML に手で項目を足さない**
   （`--check` で書かずに差分だけ見られる）。
   **localhost が要る。本番の DB には触らない**（Supabase ごと差し替える。見た目は `node shot-actual-pay.mjs` が撮る）

   ### Give → Get（2026-08-25）
   **「出した人だけが見られる」を、どこが守っているか。** 6つの約束はすべて**サーバー側**にある。
   画面側は1バイトも守っていない。

   | 約束 | 守っている場所 |
   |---|---|
   | 会員登録しただけでは見られない | [db/pay-rows.sql](db/pay-rows.sql) の `pv_pay_rows()` が `profiles.access_until` を見て、無ければ `state:'locked'` ＋ **行ゼロ**（数え上げは返す。上の★を参照）|
   | 口コミだけでは解放しない | `access_until` に書くのは給与保存の1か所だけ。口コミも招待も触らない（[db/referrals.sql:26](db/referrals.sql#L26) に明記） |
   | 給与を1件保存した人だけ解放 | [db/pay-reports.sql](db/pay-reports.sql) の `submit_pay_report` が保存に成功したときだけ書く。**手入力（`source='web'`）でも開く** |
   | 保存成功時に90日 | `greatest(coalesce(access_until,'-infinity'), now() + interval '90 days')` |
   | 採用判定と閲覧権は分離 | 返り値の `benchmark` は別に計算していて5人未満なら `null`。それでも `access_until` は立つ |
   | 期限切れ後、更新すると再解放 | 同じ `greatest(...)` が今日から90日を立て直す |

   ⚠️ **ぼかしで隠す実装を書かない。** 鍵が無い人にはサーバーが行を返していないので、隠すものが最初から無い。
   `assert-pay-rows.mjs` の A節が2重に見張っている ── ①4つのファイルに `blur(` / `filter:` /
   `backdrop-filter` / `text-security` の字が無いこと ②実際に開いた画面を歩いて
   `getComputedStyle` の `filter` が `none` でない要素が1つも無いこと（②はクラス名を変えても逃げられない）。
   ⚠️ [pv-gates.js](pv-gates.js) がやるのは**左メニューに錠前の絵を出すか決めるだけ**。
   `localStorage['pv_salary_unlock_expiry']` を**読むだけで書かない**（`assert-unlock.mjs` の③が見張る）。
   写しは古いことがあるので、確かな値を持つ画面が上書きする ──
   `actual-pay.js` は `pv_pay_rows()` の `state` から、`my-value.js` は `my_pay_reports()` の
   `access_until` から `PVGates.mark()` を呼ぶ。**`actual-pay.js` から `my_pay_reports()` は引かない**。
   ⚠️ **DEEP PAY / VERIFIED PAY は「準備中」と書く。** ページがまだ無いのに
   「詳しく出すと開きます」と書くと嘘になる。いま開くのは REAL PAY だけ。
   左メニューの2つは `<button type="button">`（`href` の無い `<a>` はキーボードから掴めず、
   本物の `href` は `assert-links.mjs` が 404 で落とす）。押すと Give → Get の説明が出る。
   その説明は**覆いではない**（`position:fixed` も `role="dialog"` も `body.style.overflow` も書かない。
   招待の着地と同じ考え方）。閉じ方は3つ ── × ／ 外を押す ／ ESC。
   ⚠️ 3段の文言は [pv-gates.js](pv-gates.js) の `giveGetHTML()` **1か所だけ**が持つ。
   ロック画面（`actual-pay.js`）はそれを借りる。2か所に書き写さない。
   ⚠️ **`pv-gates.js` は数を作らない・数えない。** 渡されなかったときだけ、
   **押されたら1回サーバーに聞く**（`pv_give_progress()`。下の「DEEP PAY の解放条件」を参照）。
   自分で計算する形に戻さない。

   ### まだ給与を出していない人が見る2画面（2026-08-25）
   オーナーが完成イメージを2枚描いて渡してきた。会員登録だけ／口コミだけの人が見る
   **①マイレポート（[my-value.html](my-value.html)）②REAL PAY（[actual-pay.html](actual-pay.html)）**。
   どちらも「明細が要る」と読める文言をやめ、**手入力でも同じように開く**ことを先に言う。

   ★**見出し・列名・ボタンにカッコの注記を足さない**（オーナー再指摘
   「実装していいけど、（丸め）とか不要な文字はいらないからね」）。
   説明が要るものは本文か、表の下の1文が引き受ける。
   `assert-pay-rows.mjs` の `noParen()` が字として見張っている
   （「（丸め）」「（約〜秒）」「（目安）」のたぐいは静かに戻るため）。
   ⚠️ **表の下の「何が載っていないか」の1文だけは消さない**（あれは注記ではなく約束）。

   - **REAL PAY のロック時** … 橙のヒーロー＋3段の表＋CTA＋右に飾りの絵、下段に
     「解放後の一覧イメージ」（**中身の無い骨組み**。数字も社名も1文字も入れない）と
     「REAL PAY で見えること」。2段組の目印は **`.ap-lock-cols` / `.ap-lock-aside`**
     （`.ap-cols` / `.ap-main` / `.ap-side` は開いている画面の禁止語）。
     ⚠️ 飾りの絵は**分布図ではない**。ロックのときだけ出す。`.ap-viz` `.ap-plot` `.ap-bar` は使わない
   - **マイレポートの未提出時**（[my-value.js](my-value.js) の `renderEmpty`）…
     「あなたの給与レポート」＋「レポートでわかること」／数え上げの帯＋数字カード3枚／
     3段の表／「給与を追加する」2枚（橙＝手入力・おすすめ、青＝明細から）。
     ★青（`--pv-blue-*`）は**この画面では使ってよい**。禁じているのは REAL PAY の「推定」の青だけ。
     数え上げのために `pv_pay_rows()` を引くのは**この空っぽの枝だけ**
     （1件でも出している人は鍵を持つので、引くと要らない行が全部返ってくる）
   - **左のプライバシーの札**（[my-value.css](my-value.css) の `.mv-priv`）は
     `<nav class="mr-side">` の**外**に置く。nav の中は8枚へ配られていて1バイトも食い違えない。
     持つのはマイレポート日英の2枚だけ。`.mr-col` が nav と札を縦に積み、
     狭い画面では札ごと消える（同じことを2度言わないので nav の中の1行も隠す）
   - 数え上げが読めないときは、**帯の1行も数字カードも丸ごと消える**（0 を並べない）。
     絵で確かめるのは `node shot-value.mjs empty-nostat ja`。
     見た目は `node shot-value.mjs empty ja` と `node shot-actual-pay.mjs locked ja` が撮る

   ### DEEP PAY の解放条件（2026-08-25 オーナー決定）
   > **Contribution determines access. Community size determines product maturity.**

   **2つとも満たしたときだけ開く。**
   ① 給与を出したユニークなパイロットが **100人**（FOUNDING PILOT 100 と同じ100人。
   「登録者100人」ではない）／
   ② **本人が内訳まで出している**（分かる項目だけでよい。全部必須にしない）

   - ★**判定は `base_pay is not null or guarantee_pay is not null
     or command_pay is not null or pay_items is not null`**（2026-08-26 に直した）。
     前は `gross_monthly is null and base_pay is not null`＝「総支給と内訳は排他」を前提にしていた。
     フォームの作り直しで**総支給と内訳が両立する**ようになったので、そのままだと
     **内訳を書いた人が全員「内訳なし」と判定される**。**過去の投稿も遡って数に入る**。
     ⚠️ **`guarantee_pay` を外さない。**基本給という項目が無く保証給だけが下限として出る会社
     （米国型）の人が、内訳を書いたのに「内訳なし」に落ちる
   - **明細を読み取れた人に、もう一度フォームを入れさせない**（読み取りが内訳の列を埋めるので
     ②は自動で満たされる。特別扱いを書かない）
   - 100人に届くまで DEEP PAY は開かない。**でも内訳の投稿は今日からできる**。
     先に出した人には「✓ あなたの準備は完了しています」＋「あと N 人…」＋招待の導線を出す
     ── **先に出した人が出し損に見えないこと**がこの表示の目的
   - ⚠️ **100人は Privacy Threshold ではない。** アクセス権とプライバシーの判定は必ず別に持つ
     （集計側の k≧5 は今も生きている）
   - `contributors` は**給与フォームを通った人だけ**を数える
     （`pay_reports` ＋ 未引き取りの預かり。**口コミ由来の7人は数えない**）。
     ★**会員登録の数ではない。**登録しただけでは N は動かず、**給与を1件出したときに1つ増える**
     （2026-08-25 オーナー決定。2026-08-26 に問われたので明記）
   - ★**数え方の唯一の正は `pv_contributors()`**（[db/pay-rows.sql](db/pay-rows.sql) の 1-f）。
     `pv_pay_rows()` の `contrib` も `pv_give_progress()` も**この関数を呼ぶだけ**で式を持たない
     （`pv_pending_usd` が `pv_annual_total` を呼ぶのと同じ形）。
     ⚠️ 書き写すと**静かに壊れる**。画面は普通に動いたまま、同じ「N / 100人」が画面ごとに違う数になる。
     `db/pay-rows.sql` の自己点検 41〜43 と `assert-pay-rows.mjs` が字として見張っている
   - 札の文言（「N / 100人」「準備中」）を持つのは [pv-gates.js](pv-gates.js) **1か所だけ**
   - ★**札は左メニューを持つ画面すべてで同じ数を出す**（2026-08-26）。
     数を渡されるのは2画面だけ ── `actual-pay.js` と `my-value.js` の空の枝が
     `pv_pay_rows()` の `stats` から `PVGates.setProgress()` を呼ぶ。
     **渡されない画面**（設定・待遇アンケート・給与を1件以上出した人のマイレポート）では、
     [pv-gates.js](pv-gates.js) が **DEEP PAY を押されたときに1回だけ** `pv_give_progress()` に聞く。
     ⚠️ ここで `pv_pay_rows()` を引かない（鍵を持つ人には要らない行が全部付いてくる）。
     `pv_give_progress()` が返すのは**整数1つと真偽3つだけ**で、行も金額も日付も社名も返さない。
     ⚠️ **ページを開いただけでは投げない**（押されたときだけ・二度目は投げない）。
     取れなければ「準備中」のまま。**0 を置いて埋めない**
   - ★**ロック画面の数字カード3枚は出したまま**（2026-08-26 オーナー確認済み・確定）
